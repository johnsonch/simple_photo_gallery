<% @files_in_directory.children.each do |file|  %>
  <%= link_to file, {:controller => "files", :action => "show", :image => "/photos/" + params[:directory_path]+ "/" + file.basename} %><br />
<% end %>
